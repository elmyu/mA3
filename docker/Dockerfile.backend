FROM python:3.12-slim

WORKDIR /app

COPY backend/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY backend/ .

EXPOSE 5000
CMD ["sh", "-c", "python seed.py && gunicorn -b 0.0.0.0:5000 --workers 2 run:app"]
